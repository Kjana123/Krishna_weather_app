<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Weather Result</title>
  <link rel="stylesheet" href="/styles.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
  <div class="container fade-in result-card">
    <h2>🌍 Weather in <span class="highlight"><%= city %>, <%= country %></span></h2>
    <div class="weather-icon">
  <img src="<%= icon %>" alt="Weather icon">
</div>

    <p class="description"><%= description %></p>

    <div class="details">
      <p>🌡️ Temperature: <strong><%= temperature %>°C</strong> / <strong><%= tempF %>°F</strong></p>
      <p>🌬️ Wind Speed: <strong><%= windspeed %> m/s</strong></p>
    </div>

    <a href="/" class="back-button">🔙 Check another city</a>
  </div>
</body>
</html>
